Host: Fedora 42 Workstation - GNOME edition

Installation went smoothly
    - Installer quite dated
    - Setting up user and stuff felt modern and easy
    - GNOME Boxes (installed through Flatpak) allows me to copy paste from the host and vice versa woooooow


rpm-ostree upgrade
systemctl reboot

googlen jetbrains toolbox
download tar.gz
extract via double click
double click on the 'jetbrains-toolbox' file inside (which is in fact an appimage)
accept the terms

install pycharm 2025.1.1

create ~/bin
toolbox app settings
screenshot
click on configure
enter /var/home/elias/bin
click apply

On Silverblue host
elias@fedora:~$ cat bin/pycharm
#!/bin/bash
#Generated by JetBrains Toolbox 2.6.2.41321 at 2025-05-10T20:19:52

"/var/home/elias/.local/share/JetBrains/Toolbox/apps/pycharm/bin/pycharm" "$@"


alternative 1: toolbox
    toolbox create fedora-42
    toolbox enter fedora-42
    sudo dnf install mpv-libs just
    mkdir git
    cd git
    git clone https://github.com/mpvqc/mpvQC.git
    python -m ensurepip
    pip3 install uv
    just build-develop
    uv run main.py

    toolbox run --container fedora-42 bin/pycharm

alternative 2: distrobox

    rpm-ostree install distrobox just
    systemctl reboot
    distrobox create --image quay.io/fedora/fedora-toolbox:42
    distrobox enter fedora-toolbox-42
    sudo dnf install mpv-libs just
    mkdir git
    cd git
    git clone https://github.com/mpvqc/mpvQC.git
    python -m ensurepip
    pip3 install uv
    just build-develop
    uv run main.py

    distrobox enter --name fedora-toolbox-42 -- bin/pycharm
